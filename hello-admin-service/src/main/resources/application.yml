spring:
  application:
    name: hello-admin-service
  profiles:
    active:
      - security
      - cache

server:
  port: 9000
  servlet:
    context-path: /hello-admin
    encoding:
      force: true
